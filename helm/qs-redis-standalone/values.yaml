# Pharia AI Quick Start values for Redis Standalone
# This file contains values that override the upstream redis chart

# All values are passed through to the redis subchart
# For full configuration options, see:
# https://github.com/OT-CONTAINER-KIT/redis-operator/blob/main/charts/redis/values.yaml

redis:
  # Redis image
  # redisStandalone:
  #   image: quay.io/opstree/redis:v7.0.12
  
  # Storage configuration
  # storage:
  #   volumeClaimTemplate:
  #     spec:
  #       accessModes: ["ReadWriteOnce"]
  #       resources:
  #         requests:
  #           storage: 1Gi
  
  # Enable monitoring
  # redisExporter:
  #   enabled: true
  #   image: quay.io/opstree/redis-exporter:v1.44.0
  
  # ServiceMonitor for Prometheus
  # serviceMonitor:
  #   enabled: false
  
  # Resources
  # kubernetesConfig:
  #   resources:
  #     requests:
  #       cpu: 100m
  #       memory: 128Mi
  #     limits:
  #       cpu: 400m
  #       memory: 512Mi
